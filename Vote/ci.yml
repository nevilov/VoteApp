image: mcr.microsoft.com/dotnet/sdk:5.0
before_script:
  - dotnet restore
  
pages:
  stage: build
  script:
  - mkdir public
  - dotnet publish -c Release -o output
  - echo Done
  #- cp -a ./Vote/output/Vote/dist/* ./public
  artifacts:
    paths:
    - public
  only:
  - master
